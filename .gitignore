# Templates
templates/

Wait, I need to reconsider. The file 'templates/base_pwa.html' is a source file being modified, not a build artifact or temporary file. It should not be ignored.

Actually, looking at the input again, only 'templates/base_pwa.html' was modified, which is a template source file. This is not a build artifact or temporary file that should be ignored.

(empty)