Nothing needs to be added to .gitignore since only a text file (`persian_translations.txt`) was added, which appears to be source content rather than build artifacts, dependencies, or temporary files.